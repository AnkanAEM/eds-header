/* eslint-disable no-use-before-define */
import { getMetadata } from '../../scripts/aem.js';
import { loadFragment } from '../fragment/fragment.js';

function createArrowSvg() {
  const svg = document.createElement('svg');
  svg.setAttribute('viewBox', '-23.5 -23.5 122.80 122.80');
  svg.setAttribute('fill', '#000000');
  svg.setAttribute('stroke', '#000000');
  svg.setAttribute('stroke-width', '4.851456000000001');
  svg.innerHTML = `
    <g id="SVGRepo_bgCarrier" stroke-width="0"></g>
    <g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round" stroke="#CCCCCC" stroke-width="0.30321600000000004"></g>
    <g id="SVGRepo_iconCarrier">
      <g id="Group_65" data-name="Group 65" transform="translate(-831.568 -384.448)">
        <path id="Path_57" data-name="Path 57" d="M833.068,460.252a1.5,1.5,0,0,1-1.061-2.561l33.557-33.56a2.53,2.53,0,0,0,0-3.564l-33.557-33.558a1.5,1.5,0,0,1,2.122-2.121l33.556,33.558a5.53,5.53,0,0,1,0,7.807l-33.557,33.56A1.5,1.5,0,0,1,833.068,460.252Z" fill="#030408"></path>
      </g>
    </g>
  `;
  return svg;
}

function buildMenu(menuHtmlString) {
  const parser = new DOMParser();
  const doc = parser.parseFromString(menuHtmlString, 'text/html');
  const rootUl = doc.body.firstElementChild; // Assuming the string starts with a <ul>

  if (!rootUl || rootUl.tagName !== 'UL') return null;

  Array.from(rootUl.querySelectorAll('li')).forEach((li) => {
    const anchor = li.querySelector('a');
    const nestedUl = li.querySelector('ul');

    if (anchor) {
      if (!anchor.href && !nestedUl) {
        // If it's a non-linked parent with no submenu, like "Technology Services"
        const text = anchor.textContent;
        const newAnchor = document.createElement('a');
        newAnchor.textContent = text;
        li.insertBefore(newAnchor, anchor);
        li.removeChild(anchor);
      }
    }

    if (nestedUl) {
      const span = document.createElement('span');
      span.appendChild(createArrowSvg());
      // Insert span right after the anchor or directly into li if no anchor
      if (anchor) {
        anchor.appendChild(span);
      } else if (li.firstChild) {
        li.insertBefore(span, li.firstChild.nextSibling);
      }
    }
  });
  return rootUl;
}

export default async function decorate(block) {
  block.textContent = '';

  // Fetch nav content from /nav fragment
  const navFragment = await loadFragment('/nav');
  if (!navFragment) return;

  const headerEl = document.createElement('header');
  headerEl.className = 'main-header with-marquee solid nav-up';

  const containerDiv = document.createElement('div');
  containerDiv.className = 'container';

  const wrapDiv = document.createElement('div');
  wrapDiv.className = 'wrap';

  // Logo
  const logoDiv = document.createElement('div');
  logoDiv.className = 'logo';
  const logoLink = document.createElement('a');
  logoLink.href = 'https://www.mahindra.com/';
  const logoImg = document.createElement('img');
  logoImg.src = 'https://www.mahindra.com//sites/default/files/2025-07/mahindra-red-logo.webp';
  logoImg.alt = 'Mahindra Brand Logo White';
  logoImg.title = 'Mahindra Brand Logo White Image';
  logoImg.className = 'hiddenlogo1';
  logoImg.width = '200';
  logoImg.height = '30';
  logoImg.loading = 'lazy';
  logoLink.appendChild(logoImg);
  logoDiv.appendChild(logoLink);
  wrapDiv.appendChild(logoDiv);

  // Hamburger (mobile toggle)
  const hamburgerDiv = document.createElement('div');
  hamburgerDiv.className = 'hamburger';
  const hamburgerUl = document.createElement('ul');
  for (let i = 0; i < 3; i += 1) {
    hamburgerUl.appendChild(document.createElement('li'));
  }
  hamburgerDiv.appendChild(hamburgerUl);
  wrapDiv.appendChild(hamburgerDiv);

  // Main Navigation
  const navElement = document.createElement('nav');
  navElement.className = 'main-nav';
  const navUl = document.createElement('ul');
  navUl.setAttribute('itemscope', '');
  navUl.setAttribute('itemtype', 'http://www.schema.org/SiteNavigationElement');

  const l1Items = JSON.parse(getMetadata('nav')); // Assuming nav-sections metadata holds the extracted data

  l1Items.forEach((item) => {
    const li = document.createElement('li');
    li.className = 'has-child hover-red';
    li.setAttribute('itemprop', 'name');

    const a = document.createElement('a');
    a.setAttribute('itemprop', 'url');
    a.href = item.l1Href;
    a.textContent = item.l1Label;

    // Add arrow SVG for L1 items
    const spanArrow = document.createElement('span');
    spanArrow.appendChild(createArrowSvg());
    a.appendChild(spanArrow);
    li.appendChild(a);

    // Create mega-menu structure
    const megaMenuDiv = document.createElement('div');
    megaMenuDiv.className = 'mega-menu';
    const megaMenuWrap = document.createElement('div');
    megaMenuWrap.className = 'wrap container';
    const centerDiv = document.createElement('div');
    centerDiv.className = 'center-div';

    // Left-div (dynamic content placeholders based on original HTML structure)
    const leftDiv = document.createElement('div');
    leftDiv.className = `left-div ${item.l1Label.toLowerCase().replace(/\s/g, '-')}-left-div`;
    const leftDivHeading = document.createElement('h4');
    const leftDivHeadingLink = document.createElement('a');
    leftDivHeadingLink.textContent = item.l1Label; // Placeholder
    leftDivHeading.appendChild(leftDivHeadingLink);
    leftDiv.appendChild(leftDivHeading);

    // Specific content based on L1 item
    if (item.l1Label === 'Who We Are') {
      const pDesc = document.createElement('p');
      pDesc.className = 'left-div-desc';
      pDesc.textContent = 'Drive positive change in the lives of our communities. Only when we enable others to rise will we rise.';
      leftDiv.appendChild(pDesc);
      const pSubDesc = document.createElement('p');
      pSubDesc.className = 'left-div-subdesc';
      pSubDesc.textContent = '#TogetherWeRise';
      leftDiv.appendChild(pSubDesc);
    } else if (item.l1Label === 'What we do') {
      const keyFactsList = document.createElement('ul');
      keyFactsList.innerHTML = `
        <li class="list-text-red">20+ <span>Industries</span></li>
        <li class="list-text-red">100+ <span>Countries</span></li>
        <li class="list-text-red">324K+ <span>Employees</span></li>
      `;
      leftDiv.appendChild(keyFactsList);
    } else if (item.l1Label === 'Investor Relations') {
      const p = document.createElement('p');
      p.textContent = 'Group Highlights - Q3 F26';
      leftDiv.appendChild(p);
      const statsList = document.createElement('ul');
      statsList.innerHTML = `
        <li class="list-text-red">20.1% <span>Consolidated ROE (Annualized)</span></li>
        <li class="list-text-red">Rs 52,100 cr <span>Revenue</span></li>
        <li class="list-text-red">Rs 4,675 cr <span>PAT</span></li>
      `;
      leftDiv.appendChild(statsList);
      leftDiv.classList.add('ir-left-div');
    } else if (item.l1Label === 'newsroom') {
      const latestPressDiv = document.createElement('div');
      latestPressDiv.className = 'latest-two-press-release';
      // Content of latest press release would be dynamically loaded or handled by another block
      leftDiv.appendChild(latestPressDiv);
      leftDiv.classList.add('newsroom-left-div');
    } else if (item.l1Label === 'careers') {
      const pDesc = document.createElement('p');
      pDesc.className = 'left-div-desc';
      pDesc.textContent = 'Committed to elevate the lives of communities, guided by our core behaviours and values.';
      leftDiv.appendChild(pDesc);
      const pSubDesc = document.createElement('p');
      pSubDesc.className = 'left-div-subdesc';
      pSubDesc.textContent = 'Bold. Agile. Collaborative.';
      leftDiv.appendChild(pSubDesc);
      leftDiv.classList.add('career-left-div');
    }

    centerDiv.appendChild(leftDiv);

    const subNavWrap = document.createElement('div');
    subNavWrap.className = `sub-nav-wrap ${item.l1Label.toLowerCase().replace(/\s/g, '-')}-sub-nav`;
    if (item.l1Label === 'Investor Relations') subNavWrap.classList.add('element-block');
    if (item.l1Label === 'careers') subNavWrap.classList.add('careers-div');

    const menuContent = buildMenu(item.menuHtml);
    if (menuContent) {
      Array.from(menuContent.children).forEach((child) => subNavWrap.appendChild(child));
    }
    // Special case for Investor Relations to include the inner-sub-nav-wrap-list div
    if (item.l1Label === 'Investor Relations') {
      const innerSubNavWrapList = document.createElement('div');
      innerSubNavWrapList.className = 'inner-sub-nav-wrap-list';
      Array.from(subNavWrap.children).slice(1, 3).forEach((child) => {
        innerSubNavWrapList.appendChild(child);
      });
      subNavWrap.insertBefore(innerSubNavWrapList, subNavWrap.children[1] || null);
    }

    centerDiv.appendChild(subNavWrap);
    megaMenuWrap.appendChild(centerDiv);
    megaMenuDiv.appendChild(megaMenuWrap);
    li.appendChild(megaMenuDiv);
    navUl.appendChild(li);
  });
  navElement.appendChild(navUl);

  // Mobile and Desktop Icon Navs (Contact Us & Search)
  const createIconNav = (type) => {
    const iconNavDiv = document.createElement('div');
    iconNavDiv.className = `icon-nav ${type}-menus-icon`;
    const ul = document.createElement('ul');

    // Contact Us
    const contactLi = document.createElement('li');
    contactLi.className = 'mail';
    const contactLink = document.createElement('a');
    contactLink.href = 'https://www.mahindra.com/contact-us';
    if (type === 'mobile') {
      contactLink.textContent = 'Contact Us';
    } else {
      // SVG for desktop
      const contactSvg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
      contactSvg.setAttribute('version', '1.1');
      contactSvg.setAttribute('id', 'Layer_1');
      contactSvg.setAttribute('x', '0px');
      contactSvg.setAttribute('y', '0px');
      contactSvg.setAttribute('viewBox', '0 0 48 38.4');
      contactSvg.setAttribute('style', 'enable-background:new 0 0 48 38.4;');
      contactSvg.setAttribute('xml:space', 'preserve');
      contactSvg.setAttribute('width', '21');
      contactSvg.setAttribute('height', '21');
      contactSvg.innerHTML = '<path d="M3.6,38.4c-1,0-1.8-0.4-2.5-1.1S0,35.8,0,34.8V3.6c0-1,0.4-1.8,1.1-2.5S2.6,0,3.6,0h40.8c1,0,1.8,0.4,2.5,1.1C47.6,1.8,48,2.6,48,3.6v31.2c0,1-0.4,1.8-1.1,2.5c-0.7,0.7-1.6,1.1-2.5,1.1H3.6z M24,20.3L3.6,6.9v27.9h40.8V6.9L24,20.3z M24,16.7L44.2,3.6H3.9L24,16.7z M3.6,6.9V3.6v31.2V6.9z" />';
      contactLink.appendChild(contactSvg);
    }
    contactLi.appendChild(contactLink);
    ul.appendChild(contactLi);

    // Search
    const searchLi = document.createElement('li');
    searchLi.className = 'search';
    const searchLink = document.createElement('a');
    searchLink.href = '#';
    const searchSvgLens = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    searchSvgLens.setAttribute('viewBox', '0 0 21 21');
    searchSvgLens.setAttribute('fill', 'none');
    searchSvgLens.className = 'lens';
    searchSvgLens.innerHTML = '<path d="M15.0934 2.73157L15.0934 2.73156C11.6883 -0.67354 6.14543 -0.67354 2.74033 2.73156C-0.666039 6.13793 -0.666063 11.6795 2.74035 15.0847C4.38993 16.7342 6.58308 17.6433 8.91623 17.6433C10.9916 17.6433 12.9533 16.9181 14.5221 15.5975L19.5217 20.5972C19.6721 20.7476 19.8687 20.8212 20.0632 20.8212C20.2588 20.8212 20.4554 20.7476 20.6059 20.5972C20.905 20.2981 20.905 19.8121 20.6059 19.513L15.6062 14.5132C18.4815 11.0845 18.3159 5.95535 15.0934 2.73157ZM14.0092 14.0004C12.6491 15.3606 10.8404 16.1098 8.91623 16.1098C6.99211 16.1098 5.18468 15.3606 3.82452 14.0004C1.01633 11.1923 1.01633 6.62394 3.82452 3.81575C5.22857 2.41171 7.07147 1.71024 8.91623 1.71024C10.7609 1.71024 12.6052 2.41296 14.0092 3.81575C16.8174 6.62394 16.8174 11.1923 14.0092 14.0004Z" stroke-width="0.25"></path>';
    searchLink.appendChild(searchSvgLens);

    const searchSvgClose = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    searchSvgClose.setAttribute('viewBox', '0 0 50 50');
    searchSvgClose.className = 'close';
    searchSvgClose.innerHTML = '<path d="M 9.15625 6.3125 L 6.3125 9.15625 L 22.15625 25 L 6.21875 40.96875 L 9.03125 43.78125 L 25 27.84375 L 40.9375 43.78125 L 43.78125 40.9375 L 27.84375 25 L 43.6875 9.15625 L 40.84375 6.3125 L 25 22.15625 Z"></path>';
    searchLink.appendChild(searchSvgClose);

    if (type === 'mobile') {
      const searchSpan = document.createElement('span');
      searchSpan.textContent = ' Search';
      searchLink.appendChild(searchSpan);
    }
    searchLi.appendChild(searchLink);

    // Search screen wrap - this is a complex dynamic element. Providing basic structure.
    const searchScreenWrap = document.createElement('div');
    searchScreenWrap.className = 'search-screen-wrap';
    searchScreenWrap.innerHTML = `
      <div class="wrap">
        <form action="https://www.mahindra.com/search" method="get" id="search-block-form" accept-charset="UTF-8">
          <div class="search-wrap">
            <div class="search-icon"><svg viewBox="0 0 21 21" fill="none"><path d="M15.0934 2.73157L15.0934 2.73156C11.6883 -0.67354 6.14543 -0.67354 2.74033 2.73156C-0.666039 6.13793 -0.666063 11.6795 2.74035 15.0847C4.38993 16.7342 6.58308 17.6433 8.91623 17.6433C10.9916 17.6433 12.9533 16.9181 14.5221 15.5975L19.5217 20.5972C19.6721 20.7476 19.8687 20.8212 20.0632 20.8212C20.2588 20.8212 20.4554 20.7476 20.6059 20.5972C20.905 20.2981 20.905 19.8121 20.6059 19.513L15.6062 14.5132C18.4815 11.0845 18.3159 5.95535 15.0934 2.73157ZM14.0092 14.0004C12.6491 15.3606 10.8404 16.1098 8.91623 16.1098C6.99211 16.1098 5.18468 15.3606 3.82452 14.0004C1.01633 11.1923 1.01633 6.62394 3.82452 3.81575C5.22857 2.41171 7.07147 1.71024 8.91623 1.71024C10.7609 1.71024 12.6052 2.41296 14.0092 3.81575C16.8174 6.62394 16.8174 11.1923 14.0092 14.0004Z" stroke-width="0.25"></path></svg></div>
            <input type="text" class="input-text searchtext" required="" name="key" id="searchInput" autocomplete="off">
            <button class="submit-button"><div class="label"> Submit </div><svg width="12" height="8" viewBox="0 0 12 8" fill="none"><path d="M11.3536 4.35355C11.5488 4.15829 11.5488 3.84171 11.3536 3.64645L8.17157 0.464465C7.97631 0.269203 7.65973 0.269203 7.46447 0.464465C7.2692 0.659728 7.2692 0.97631 7.46447 1.17157L10.2929 4L7.46447 6.82843C7.2692 7.02369 7.2692 7.34027 7.46447 7.53553C7.65973 7.7308 7.97631 7.7308 8.17157 7.53553L11.3536 4.35355ZM4.37114e-08 4.5L11 4.5L11 3.5L-4.37114e-08 3.5L4.37114e-08 4.5Z" fill="black"></path></svg></button>
          </div>
          <div class="searchResultBox" style="display: none;"><div class="swiper scrollSwiper"><div class="swiper-wrapper"><div class="swiper-slide"></div></div></div><div class="swiper-scrollbar"></div></div>
        </form>
        <div class="search-suggestions-wrap"><div class="label">Popular Keywords:</div><div class="tokens-wrap"><ul><li>Business</li><li>FY 21</li><li>Brands</li><li>XUV700</li><li>Global</li><li>Nanhi Kali</li></ul></div></div>
        <div class="search-suggestions-wrap"><div class="label">Recommended for you:</div><div class="tokens-wrap"><ul><li>Annual Report 2021 - 2022</li><li>Leadership Announcement</li><li>Latest Press Release</li><li>Brand Guidelines</li></ul></div></div>
      </div>`;
    searchLi.appendChild(searchScreenWrap);

    ul.appendChild(searchLi);
    iconNavDiv.appendChild(ul);
    return iconNavDiv;
  };

  navElement.appendChild(createIconNav('mobile'));
  navElement.appendChild(createIconNav('desktop'));

  // 80th Year Logo (outside main nav, but within wrap)
  const year80LogoDiv = document.createElement('div');
  year80LogoDiv.className = 'logo year-80-logo';
  const year80LogoLink = document.createElement('a');
  year80LogoLink.href = 'https://www.mahindra.com/';
  const year80LogoImg = document.createElement('img');
  year80LogoImg.src = 'https://www.mahindra.com/sites/default/files/2026-03/80thYearLogo_Gold_com.webp';
  year80LogoImg.alt = '80th Year Logo Gold';
  year80LogoImg.title = '80thYearLogo_Gold';
  year80LogoImg.className = 'hiddenlogo1 years-80';
  year80LogoImg.width = '74';
  year80LogoImg.height = '60';
  year80LogoImg.loading = 'lazy';
  year80LogoLink.appendChild(year80LogoImg);
  year80LogoDiv.appendChild(year80LogoLink);
  wrapDiv.appendChild(year80LogoDiv);

  containerDiv.appendChild(wrapDiv);
  headerEl.appendChild(containerDiv);
  block.appendChild(headerEl);

  // Add event listeners similar to data-once functionality
  // This would typically involve a dedicated JS file for header interactivity
  // For brevity, basic toggles are not implemented here but would follow EDS best practices.
}
